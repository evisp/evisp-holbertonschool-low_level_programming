#include "main.h"

void print_square(int size)
{
	int width, height;

	for (width = 0; width < size; width++)
	{
		for (height = 0; height < size; height++)
			_putchar('#');
		_putchar('\n');
	}
	if (size < 1)
		_putchar('\n');
}
